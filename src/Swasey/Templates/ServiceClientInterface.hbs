public interface I{{UCFirst Name}} : IServiceClient
{
{{#each Operations}}

        {{#if HasDescription}}
        /// <summary>
        ///     {{{Description}}}
        /// </summary>
        {{/if}}
        Task{{#unless Response.DataType.IsVoid}}<{{Response.DataType}}>{{/unless}} {{Name}}();
{{/each}}
}